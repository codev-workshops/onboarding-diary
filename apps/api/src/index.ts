/**
 * API entrypoint. The Express application is introduced by Epic 3 (T-030 onwards); for
 * now this module only exposes the service name so the workspace builds and is testable.
 */

export const SERVICE_NAME = 'onboarding-diary-api';
