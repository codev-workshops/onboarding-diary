/**
 * Web app entrypoint. The Vite + React application is scaffolded by Epic 11 (T-110);
 * for now this module only exposes the app name so the workspace builds and is testable.
 */

export const APP_NAME = 'onboarding-diary-web';
